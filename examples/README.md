# Examples

The files in this directory are **portfolio reference examples**, not artifacts recovered from a deployed ParkPing system.

- `mqtt-message.example.json` shows one possible compact occupancy-event schema.
- `dynamodb-item.example.json` shows one possible current-state record shape.

They exist to make the architecture concrete while preserving the repository's evidence boundary.